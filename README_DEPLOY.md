# Workout Tracker vNext

Replace only `index.html` and `sw.js` in the existing GitHub Pages repository. Keep the existing `config.js` with your Supabase URL and publishable key. Keep `manifest.json` and `icon-192.png` or replace them with these copies.

New in this build: multi-program library, per-exercise progression models, 5/3/1 main-lift engine, approval-based progression review, superset-aware workout UI with rest timer, bodyweight support, exercise library/history/PRs, 5/3/1 cycle history, workout editing/deletion, program duplication, substitutions, warm-up sets, session duration, notes, and JSON backup/restore.
